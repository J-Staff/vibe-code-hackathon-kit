# Copy values into ~/.vibe/.env (NOT into this repo). setup.sh does this for you.
MISTRAL_API_KEY=
# Optional, only if you enable the matching MCP server:
GITHUB_PAT=
FIRECRAWL_API_KEY=
TAVILY_API_KEY=
STRIPE_SECRET_KEY=
